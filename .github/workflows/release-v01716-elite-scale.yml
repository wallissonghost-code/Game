name: Caos Live v0.17.16 Elite Scale
on:
  push:
    branches: [main]
    paths:
      - 'scripts/release-v01716-elite-scale.mjs'
      - '.github/workflows/release-v01716-elite-scale.yml'
permissions:
  contents: write
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Apply Elite scale release
        run: node scripts/release-v01716-elite-scale.mjs
      - name: Validate JavaScript
        run: |
          node --check src/game.js
          node --check src/panel.js
      - name: Validate project
        run: node scripts/check-game.mjs
      - name: Commit release
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/game.js index.html painel.html version.json scripts/check-game.mjs
          git commit -m "fix: enlarge Elite Ogre visual scale v0.17.16"
          git push
