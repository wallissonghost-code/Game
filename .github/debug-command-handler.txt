
-- command #1 @ 25212 --
ue}if(lv>0)sk.apply(lv);else{if(id==='speed')player.speed=255;if(id==='rapid')player.fireRate=.28;if(id==='regen')player.regen=0;if(id==='armor')player.armorReduction=0;if(id==='xp')player.xpMult=1;if(id==='blood'){player.bloodChance=0;player.bloodHeal=0}if(id==='flash')player.flashDamage=0;if(id==='arc'){arcNextAt=0;arcFx=[]}}return true}function command(d){try{window.dispatchEvent(new CustomEvent('caos:admin-command',{detail:d}))}catch{}const c=d?.command;if(c==='pause')setPaused(true);if(c==='resume')setPaused(false);if(c==='heal')player.life=Math.min(player.maxLife,player.life+(+d.amount||1));if(c==='damage')player.life=Math.max(0,player.life-(+d.amount||2));if(c==='spawn')for(let i=0;i<Math.min(100,+d.amount||25);i++){const before=enemies.length;spawn(d.mob||null);if(enemies.length>before&&d.user)enemies[enemies.length-1].username=String(d.user).replace(/^@/,'')}if(c==='boss')boss(d.mob||null);if(c==='clear'){enemies=[];bullets=[]}if(c==='freeze')freezeUntil=performance.now()+(+d.seconds||8)*1000;if(c==='invincible')invincibleUntil=performance.now()+(+d.seconds||10)*1000;if(c==='speed')enemySpeed=Math.max(.2,Math.min(3,+d.value||1));if(c==='xp')gainXP(+d.amount||50);if(c==='level'){for(let i=0;i<(+d.amount||1);i++){xp=xpNeed;

-- command #2 @ 25276 --
(id==='rapid')player.fireRate=.28;if(id==='regen')player.regen=0;if(id==='armor')player.armorReduction=0;if(id==='xp')player.xpMult=1;if(id==='blood'){player.bloodChance=0;player.bloodHeal=0}if(id==='flash')player.flashDamage=0;if(id==='arc'){arcNextAt=0;arcFx=[]}}return true}function command(d){try{window.dispatchEvent(new CustomEvent('caos:admin-command',{detail:d}))}catch{}const c=d?.command;if(c==='pause')setPaused(true);if(c==='resume')setPaused(false);if(c==='heal')player.life=Math.min(player.maxLife,player.life+(+d.amount||1));if(c==='damage')player.life=Math.max(0,player.life-(+d.amount||2));if(c==='spawn')for(let i=0;i<Math.min(100,+d.amount||25);i++){const before=enemies.length;spawn(d.mob||null);if(enemies.length>before&&d.user)enemies[enemies.length-1].username=String(d.user).replace(/^@/,'')}if(c==='boss')boss(d.mob||null);if(c==='clear'){enemies=[];bullets=[]}if(c==='freeze')freezeUntil=performance.now()+(+d.seconds||8)*1000;if(c==='invincible')invincibleUntil=performance.now()+(+d.seconds||10)*1000;if(c==='speed')enemySpeed=Math.max(.2,Math.min(3,+d.value||1));if(c==='xp')gainXP(+d.amount||50);if(c==='level'){for(let i=0;i<(+d.amount||1);i++){xp=xpNeed;gainXP(0);if(choosing)break}}if(c==='auto'){autoMode=!!d.value;p

-- command #3 @ 25316 --
=='regen')player.regen=0;if(id==='armor')player.armorReduction=0;if(id==='xp')player.xpMult=1;if(id==='blood'){player.bloodChance=0;player.bloodHeal=0}if(id==='flash')player.flashDamage=0;if(id==='arc'){arcNextAt=0;arcFx=[]}}return true}function command(d){try{window.dispatchEvent(new CustomEvent('caos:admin-command',{detail:d}))}catch{}const c=d?.command;if(c==='pause')setPaused(true);if(c==='resume')setPaused(false);if(c==='heal')player.life=Math.min(player.maxLife,player.life+(+d.amount||1));if(c==='damage')player.life=Math.max(0,player.life-(+d.amount||2));if(c==='spawn')for(let i=0;i<Math.min(100,+d.amount||25);i++){const before=enemies.length;spawn(d.mob||null);if(enemies.length>before&&d.user)enemies[enemies.length-1].username=String(d.user).replace(/^@/,'')}if(c==='boss')boss(d.mob||null);if(c==='clear'){enemies=[];bullets=[]}if(c==='freeze')freezeUntil=performance.now()+(+d.seconds||8)*1000;if(c==='invincible')invincibleUntil=performance.now()+(+d.seconds||10)*1000;if(c==='speed')enemySpeed=Math.max(.2,Math.min(3,+d.value||1));if(c==='xp')gainXP(+d.amount||50);if(c==='level'){for(let i=0;i<(+d.amount||1);i++){xp=xpNeed;gainXP(0);if(choosing)break}}if(c==='auto'){autoMode=!!d.value;pointer=null;clearAutoTarget()}if(c==='au

-- command #4 @ 49472 --
ew Peer('chaos-live-'+room.toLowerCase(),{debug:0});peer.on('open',()=>{if(gen===peerGeneration)status('AGUARDANDO PAINEL')});peer.on('connection',c=>{if(gen!==peerGeneration){c.close();return}connections.add(c);c.on('open',()=>{if(gen===peerGeneration){status('CONECTADO',true);c.send(state())}});c.on('data',d=>{if(gen===peerGeneration&&d?.type==='command')command(d)});c.on('close',()=>{connections.delete(c);if(gen===peerGeneration&&connections.size===0)status('AGUARDANDO PAINEL')});c.on('error',()=>connections.delete(c))});peer.on('error',e=>{if(gen!==peerGeneration||e?.type==='unavailable-id')return;status('RECONECTANDO...');scheduleGamePeer(gen)});peer.on('disconnected',()=>{if(gen!==peerGeneration)return;try{peer.reconnect()}catch{scheduleGamePeer(gen)}})}setInterval(broadcast,500);openPeer();resize();draw()})();
</script><script data-caos-fps-v0106>(()=>{'use strict';let visible=true,frames=0,last=performance.now(),fps=0;try{visible=localStorage.getItem('caos-show-fps')!=='0'}catch{}let el=document.getElementById('fpsMini');if(!el){const host=document.getElementById('connectionMini');if(host){el=document.createElement('div');el.id='fpsMini';el.style.marginTop='3px';host.parentNode.insertBefore(el,host.nextSibling)}}function p

-- command #5 @ 49481 --
chaos-live-'+room.toLowerCase(),{debug:0});peer.on('open',()=>{if(gen===peerGeneration)status('AGUARDANDO PAINEL')});peer.on('connection',c=>{if(gen!==peerGeneration){c.close();return}connections.add(c);c.on('open',()=>{if(gen===peerGeneration){status('CONECTADO',true);c.send(state())}});c.on('data',d=>{if(gen===peerGeneration&&d?.type==='command')command(d)});c.on('close',()=>{connections.delete(c);if(gen===peerGeneration&&connections.size===0)status('AGUARDANDO PAINEL')});c.on('error',()=>connections.delete(c))});peer.on('error',e=>{if(gen!==peerGeneration||e?.type==='unavailable-id')return;status('RECONECTANDO...');scheduleGamePeer(gen)});peer.on('disconnected',()=>{if(gen!==peerGeneration)return;try{peer.reconnect()}catch{scheduleGamePeer(gen)}})}setInterval(broadcast,500);openPeer();resize();draw()})();
</script><script data-caos-fps-v0106>(()=>{'use strict';let visible=true,frames=0,last=performance.now(),fps=0;try{visible=localStorage.getItem('caos-show-fps')!=='0'}catch{}let el=document.getElementById('fpsMini');if(!el){const host=document.getElementById('connectionMini');if(host){el=document.createElement('div');el.id='fpsMini';el.style.marginTop='3px';host.parentNode.insertBefore(el,host.nextSibling)}}function paint(){if

-- command #6 @ 50641 --
yle.marginTop='3px';host.parentNode.insertBefore(el,host.nextSibling)}}function paint(){if(el){el.style.display=visible?'block':'none';el.textContent='FPS: '+(fps||'--')}}function tick(t){frames++;if(t-last>=500){fps=Math.max(0,Math.round(frames*1000/(t-last)));frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){visible=!!d.value;try{localStorage.setItem('caos-show-fps',visible?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();</script><script data-caos-fps-hud-v109>(()=>{'use strict';let on=true,frames=0,last=performance.now(),fps=0;try{on=localStorage.getItem('caos-show-fps')!=='0'}catch{}const el=document.getElementById('fpsHud');function paint(){if(!el)return;el.style.display=on?'inline-block':'none';el.textContent='FPS '+(fps||'--')}function tick(t){frames++;if(t-last>=500){fps=Math.round(frames*1000/(t-last));window.caosCurrentFps=fps;frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){on=!!d.value;try{localStorage.setItem('caos-show-fps',on?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();</script></body></ht

-- command #7 @ 50680 --
ertBefore(el,host.nextSibling)}}function paint(){if(el){el.style.display=visible?'block':'none';el.textContent='FPS: '+(fps||'--')}}function tick(t){frames++;if(t-last>=500){fps=Math.max(0,Math.round(frames*1000/(t-last)));frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){visible=!!d.value;try{localStorage.setItem('caos-show-fps',visible?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();</script><script data-caos-fps-hud-v109>(()=>{'use strict';let on=true,frames=0,last=performance.now(),fps=0;try{on=localStorage.getItem('caos-show-fps')!=='0'}catch{}const el=document.getElementById('fpsHud');function paint(){if(!el)return;el.style.display=on?'inline-block':'none';el.textContent='FPS '+(fps||'--')}function tick(t){frames++;if(t-last>=500){fps=Math.round(frames*1000/(t-last));window.caosCurrentFps=fps;frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){on=!!d.value;try{localStorage.setItem('caos-show-fps',on?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();</script></body></html>

-- command #8 @ 51341 --
catch{}const el=document.getElementById('fpsHud');function paint(){if(!el)return;el.style.display=on?'inline-block':'none';el.textContent='FPS '+(fps||'--')}function tick(t){frames++;if(t-last>=500){fps=Math.round(frames*1000/(t-last));window.caosCurrentFps=fps;frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){on=!!d.value;try{localStorage.setItem('caos-show-fps',on?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();</script></body></html>

-- command #9 @ 51380 --
('fpsHud');function paint(){if(!el)return;el.style.display=on?'inline-block':'none';el.textContent='FPS '+(fps||'--')}function tick(t){frames++;if(t-last>=500){fps=Math.round(frames*1000/(t-last));window.caosCurrentFps=fps;frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){on=!!d.value;try{localStorage.setItem('caos-show-fps',on?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();</script></body></html>

-- d.command #1 @ 50678 --
nsertBefore(el,host.nextSibling)}}function paint(){if(el){el.style.display=visible?'block':'none';el.textContent='FPS: '+(fps||'--')}}function tick(t){frames++;if(t-last>=500){fps=Math.max(0,Math.round(frames*1000/(t-last)));frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){visible=!!d.value;try{localStorage.setItem('caos-show-fps',visible?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();</script><script data-caos-fps-hud-v109>(()=>{'use strict';let on=true,frames=0,last=performance.now(),fps=0;try{on=localStorage.getItem('caos-show-fps')!=='0'}catch{}const el=document.getElementById('fpsHud');function paint(){if(!el)return;el.style.display=on?'inline-block':'none';el.textContent='FPS '+(fps||'--')}function tick(t){frames++;if(t-last>=500){fps=Math.round(frames*1000/(t-last));window.caosCurrentFps=fps;frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){on=!!d.value;try{localStorage.setItem('caos-show-fps',on?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();</script></body></html>

-- d.command #2 @ 51378 --
Id('fpsHud');function paint(){if(!el)return;el.style.display=on?'inline-block':'none';el.textContent='FPS '+(fps||'--')}function tick(t){frames++;if(t-last>=500){fps=Math.round(frames*1000/(t-last));window.caosCurrentFps=fps;frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){on=!!d.value;try{localStorage.setItem('caos-show-fps',on?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();</script></body></html>

-- spawn( #1 @ 16999 --
ist,y:player.y+Math.sin(a)*dist,type,tier,r:c.r*(tier===2?1.12:tier===1?1.06:1),speed:(c.s+Math.random()*8)*(tier===2?1.12:tier===1?1.06:1),hp:Math.ceil(c.h*mult),max:Math.ceil(c.h*mult),damage:Math.ceil(c.d*(tier===2?1.4:tier===1?1.18:1)),xp:Math.ceil(c.x*mult),dead:false,t:Math.random()*8,seed:Math.random()*99,attackAt:0,attackFlash:0})}function spawn(type){if(type)return makeEnemy(type,true);const pool=['wraith','reaper','infected','crawler','eye','brute'];makeEnemy(pool[Math.floor(Math.random()*pool.length)])}function spawnWave(){waveCount++;const n=Math.min(11,3+Math.floor(level/2));for(let i=0;i<n;i++)spawn()}function boss(type){makeEnemy(type||(((Math.floor(level/10))%2)?'colossus':'voidlord'),true)}
function gainXP(v){xp+=v*player.xpMult;while(xp>=xpNeed){xp-=xpNeed;level++;xpNeed=Math.floor(60*Math.pow(level,1.42));if(level%10===0)boss(((level/10)%2===0)?'voidlord':'colossus');chooseSkill();break}ui()}function weightedPick(pool){let total=pool.reduce((s,x)=>s+rarityWeight[x.r],0),r=Math.random()*total;for(const x of pool){r-=rarityWeight[x.r];if(r<=0)return x}return pool[0]}function chooseSkill(){const pool=skills.filter(s=>s.id==='phoenix'?(!phoenixConsumed&&skillLv[s.id]<1):s.id==='armor'?skillLv[s.id]<4:skillLv[s.id]<5

-- spawn( #2 @ 17264 --
ad:false,t:Math.random()*8,seed:Math.random()*99,attackAt:0,attackFlash:0})}function spawn(type){if(type)return makeEnemy(type,true);const pool=['wraith','reaper','infected','crawler','eye','brute'];makeEnemy(pool[Math.floor(Math.random()*pool.length)])}function spawnWave(){waveCount++;const n=Math.min(11,3+Math.floor(level/2));for(let i=0;i<n;i++)spawn()}function boss(type){makeEnemy(type||(((Math.floor(level/10))%2)?'colossus':'voidlord'),true)}
function gainXP(v){xp+=v*player.xpMult;while(xp>=xpNeed){xp-=xpNeed;level++;xpNeed=Math.floor(60*Math.pow(level,1.42));if(level%10===0)boss(((level/10)%2===0)?'voidlord':'colossus');chooseSkill();break}ui()}function weightedPick(pool){let total=pool.reduce((s,x)=>s+rarityWeight[x.r],0),r=Math.random()*total;for(const x of pool){r-=rarityWeight[x.r];if(r<=0)return x}return pool[0]}function chooseSkill(){const pool=skills.filter(s=>s.id==='phoenix'?(!phoenixConsumed&&skillLv[s.id]<1):s.id==='armor'?skillLv[s.id]<4:skillLv[s.id]<5);if(!pool.length){toast('TODAS AS SKILLS ESTÃO NO LV5');return}choosing=true;const available=[...pool],pick=[];while(pick.length<Math.min(3,pool.length)){const s=weightedPick(available);pick.push(s);available.splice(available.indexOf(s),1)}if(autoMode){const score

-- spawn( #3 @ 25623 --
in-command',{detail:d}))}catch{}const c=d?.command;if(c==='pause')setPaused(true);if(c==='resume')setPaused(false);if(c==='heal')player.life=Math.min(player.maxLife,player.life+(+d.amount||1));if(c==='damage')player.life=Math.max(0,player.life-(+d.amount||2));if(c==='spawn')for(let i=0;i<Math.min(100,+d.amount||25);i++){const before=enemies.length;spawn(d.mob||null);if(enemies.length>before&&d.user)enemies[enemies.length-1].username=String(d.user).replace(/^@/,'')}if(c==='boss')boss(d.mob||null);if(c==='clear'){enemies=[];bullets=[]}if(c==='freeze')freezeUntil=performance.now()+(+d.seconds||8)*1000;if(c==='invincible')invincibleUntil=performance.now()+(+d.seconds||10)*1000;if(c==='speed')enemySpeed=Math.max(.2,Math.min(3,+d.value||1));if(c==='xp')gainXP(+d.amount||50);if(c==='level'){for(let i=0;i<(+d.amount||1);i++){xp=xpNeed;gainXP(0);if(choosing)break}}if(c==='auto'){autoMode=!!d.value;pointer=null;clearAutoTarget()}if(c==='autofire'){autoFire=!!d.value;player.shotFlash=0;shotTimer=.05;toast('🔫 TIRO AUTOMÁTICO '+(autoFire?'ON':'OFF'))}if(c==='horde'){hordeEnabled=!!d.value;spawnTimer=.1;toast('🌊 HORDAS '+(hordeEnabled?'ON':'OFF'))}if(c==='skilltest'){const id=String(d.skill||''),lv=id==='phoenix'?1:id==='armor'?Math.max(1,Math.

-- boss( #1 @ 17281 --
andom()*8,seed:Math.random()*99,attackAt:0,attackFlash:0})}function spawn(type){if(type)return makeEnemy(type,true);const pool=['wraith','reaper','infected','crawler','eye','brute'];makeEnemy(pool[Math.floor(Math.random()*pool.length)])}function spawnWave(){waveCount++;const n=Math.min(11,3+Math.floor(level/2));for(let i=0;i<n;i++)spawn()}function boss(type){makeEnemy(type||(((Math.floor(level/10))%2)?'colossus':'voidlord'),true)}
function gainXP(v){xp+=v*player.xpMult;while(xp>=xpNeed){xp-=xpNeed;level++;xpNeed=Math.floor(60*Math.pow(level,1.42));if(level%10===0)boss(((level/10)%2===0)?'voidlord':'colossus');chooseSkill();break}ui()}function weightedPick(pool){let total=pool.reduce((s,x)=>s+rarityWeight[x.r],0),r=Math.random()*total;for(const x of pool){r-=rarityWeight[x.r];if(r<=0)return x}return pool[0]}function chooseSkill(){const pool=skills.filter(s=>s.id==='phoenix'?(!phoenixConsumed&&skillLv[s.id]<1):s.id==='armor'?skillLv[s.id]<4:skillLv[s.id]<5);if(!pool.length){toast('TODAS AS SKILLS ESTÃO NO LV5');return}choosing=true;const available=[...pool],pick=[];while(pick.length<Math.min(3,pool.length)){const s=weightedPick(available);pick.push(s);available.splice(available.indexOf(s),1)}if(autoMode){const scoreAuto=x=>{const id

-- boss( #2 @ 17501 --
)*pool.length)])}function spawnWave(){waveCount++;const n=Math.min(11,3+Math.floor(level/2));for(let i=0;i<n;i++)spawn()}function boss(type){makeEnemy(type||(((Math.floor(level/10))%2)?'colossus':'voidlord'),true)}
function gainXP(v){xp+=v*player.xpMult;while(xp>=xpNeed){xp-=xpNeed;level++;xpNeed=Math.floor(60*Math.pow(level,1.42));if(level%10===0)boss(((level/10)%2===0)?'voidlord':'colossus');chooseSkill();break}ui()}function weightedPick(pool){let total=pool.reduce((s,x)=>s+rarityWeight[x.r],0),r=Math.random()*total;for(const x of pool){r-=rarityWeight[x.r];if(r<=0)return x}return pool[0]}function chooseSkill(){const pool=skills.filter(s=>s.id==='phoenix'?(!phoenixConsumed&&skillLv[s.id]<1):s.id==='armor'?skillLv[s.id]<4:skillLv[s.id]<5);if(!pool.length){toast('TODAS AS SKILLS ESTÃO NO LV5');return}choosing=true;const available=[...pool],pick=[];while(pick.length<Math.min(3,pool.length)){const s=weightedPick(available);pick.push(s);available.splice(available.indexOf(s),1)}if(autoMode){const scoreAuto=x=>{const id=x.id,cur=skillLv[id]||0;if(id==='phoenix')return phoenixConsumed?-999:120;if(id==='medic')return player.life/player.maxLife<.55?110:72;if(id==='regen')return player.life/player.maxLife<.7?92:68;if(id==='arc')return 88-c

-- boss( #3 @ 25756 --
er.life=Math.min(player.maxLife,player.life+(+d.amount||1));if(c==='damage')player.life=Math.max(0,player.life-(+d.amount||2));if(c==='spawn')for(let i=0;i<Math.min(100,+d.amount||25);i++){const before=enemies.length;spawn(d.mob||null);if(enemies.length>before&&d.user)enemies[enemies.length-1].username=String(d.user).replace(/^@/,'')}if(c==='boss')boss(d.mob||null);if(c==='clear'){enemies=[];bullets=[]}if(c==='freeze')freezeUntil=performance.now()+(+d.seconds||8)*1000;if(c==='invincible')invincibleUntil=performance.now()+(+d.seconds||10)*1000;if(c==='speed')enemySpeed=Math.max(.2,Math.min(3,+d.value||1));if(c==='xp')gainXP(+d.amount||50);if(c==='level'){for(let i=0;i<(+d.amount||1);i++){xp=xpNeed;gainXP(0);if(choosing)break}}if(c==='auto'){autoMode=!!d.value;pointer=null;clearAutoTarget()}if(c==='autofire'){autoFire=!!d.value;player.shotFlash=0;shotTimer=.05;toast('🔫 TIRO AUTOMÁTICO '+(autoFire?'ON':'OFF'))}if(c==='horde'){hordeEnabled=!!d.value;spawnTimer=.1;toast('🌊 HORDAS '+(hordeEnabled?'ON':'OFF'))}if(c==='skilltest'){const id=String(d.skill||''),lv=id==='phoenix'?1:id==='armor'?Math.max(1,Math.min(4,+d.level||1)):Math.max(0,Math.min(5,+d.level||1));if(adminSkillApply(id,lv))toast('🧪 '+id.toUpperCase()+' · LV '+lv)}if(c==='sk
