(()=>{'use strict';const VERSION='0.17.16',ASSET_TAG=VERSION.replace(/\./g,''),$=id=>document.getElementById(id),canvas=$('canvas'),stage=$('stage'),ctx=canvas.getContext('2d',{alpha:false}),keys={};let W=0,H=0,dpr=1,running=false,paused=false,choosing=false,last=0,raf=0,pointer=null,score=0,level=1,xp=0,xpNeed=60,spawnTimer=0,shotTimer=0,enemies=[],bullets=[],enemySpeed=1,freezeUntil=0,invincibleUntil=0,flashCounter=0,autoMode=false,autoAngle=0,autoTurn=0,hordeEnabled=true,killCount=0,medDrop=null,nextMedDropAt=0,toastText='',toastUntil=0,arcNextAt=0,arcFx=[],phoenixReady=false,phoenixConsumed=false,phoenixShieldUntil=0,perfMode=0,perfFrames=0,perfWindowStart=performance.now(),perfLastFps=60,renderScale=1,frameSeq=0,autoFire=true,gameplayMode='classic',movementAimAngle=0,runStartedAt=0,waveCount=0,deathState=null,autoTarget=null,autoTargetUntil=0,autoMoveX=0,autoMoveY=0,autoDecisionUntil=0,lastDamageAt=0,regenStage=0,aimTarget=0,damageFx=[],pierceShotCounter=0,nextWaveAt=0;const skillLv={speed:0,medic:0,rapid:0,xp:0,flash:0,regen:0,blood:0,arc:0,phoenix:0,armor:0,pierce:0};const player={x:0,y:0,r:18,speed:255,life:100,maxLife:100,inv:0,moving:false,walk:0,aim:0,shotFlash:0,fireRate:.28,xpMult:1,regen:0,flashDamage:0,bloodChance:0,bloodHeal:0,damage:2,armorReduction:0};const MAX_ENEMIES=320,GRID=64,CHUNK=640;
const types={wraith:{name:'Ogro Espectro',r:15,s:96,h:3,d:2,x:7,c:'#7c3aed'},reaper:{name:'Ogro Ceifador',r:21,s:67,h:8,d:4,x:18,c:'#0e7490'},infected:{name:'Ogro Infectado',r:17,s:76,h:5,d:3,x:10,c:'#4d7c0f'},crawler:{name:'Ogro das Sombras',r:16,s:124,h:4,d:2,x:9,c:'#1e293b'},eye:{name:'Ogro Observador',r:16,s:86,h:5,d:2,x:11,c:'#b91c1c'},brute:{name:'Ogro Brutamonte',r:24,s:54,h:14,d:5,x:24,c:'#92400e'},colossus:{name:'Ogro Colosso',r:42,s:42,h:100,d:8,x:150,c:'#991b1b',boss:1},voidlord:{name:'Ogro do Vazio',r:39,s:55,h:100,d:7,x:170,c:'#312e81',boss:1}};
const rarityLabel={common:'COMUM',rare:'RARA',epic:'ÉPICA',legendary:'LENDÁRIA',secret:'SECRETA'},rarityWeight={common:70,rare:25,epic:9,legendary:3,secret:.35};const skills=[{id:'speed',n:'Passos de Guerra',i:'🥾',r:'common',desc:l=>`Velocidade total +${l*10}%.`,apply:l=>player.speed=255*(1+l*.10)},{id:'medic',n:'Kit Médico',i:'🩹',r:'common',desc:l=>`+${l*10} de vida máxima; ao evoluir cura 25 HP.`,apply:l=>{player.maxLife+=10;player.life=Math.min(player.maxLife,player.life+25)}},{id:'rapid',n:'Rajada Rápida',i:'⚡',r:'common',desc:l=>`Cadência +${[0,10,20,30,45,60][l]}%.`,apply:l=>player.fireRate=.28*(1-[0,.10,.20,.30,.45,.60][l])},{id:'regen',n:'Regeneração',i:'💚',r:'rare',desc:l=>`Regen progressiva: ${[0,.4,.6,.8,1,1.2][l]} HP/s base; acelera após 3s, 6s e 10s sem sofrer dano.`,apply:l=>player.regen=[0,.4,.6,.8,1,1.2][l]},{id:'armor',n:'Armadura',i:'🛡️',r:'rare',desc:l=>`${['','Bronze','Prata','Ouro','Diamante'][l]} · reduz ${[0,5,9,13,18][l]}% do dano recebido por criaturas.`,apply:l=>player.armorReduction=[0,.05,.09,.13,.18][l]},{id:'xp',n:'Instinto de Caça',i:'✨',r:'rare',desc:l=>`Recebe +${l*20}% de XP.`,apply:l=>player.xpMult=1+l*.20},{id:'blood',n:'Sanguinário',i:'🩸',r:'epic',desc:l=>`A cada 10 abates: ${[0,10,12,15,18,20][l]}% de +1 HP máximo; resto cura ${String([0,.5,.75,1,1.5,2][l]).replace('.',',')} HP.`,apply:l=>{player.bloodChance=[0,.10,.12,.15,.18,.20][l];player.bloodHeal=[0,.5,.75,1,1.5,2][l]}},{id:'flash',n:'Flash de Luz',i:'☀️',r:'legendary',desc:l=>`Feixe perfurante de ${[0,8,10,12,15,18][l]} de dano a cada 5 tiros.`,apply:l=>player.flashDamage=[0,8,10,12,15,18][l]},{id:'arc',n:'Arco Voltaico',i:'⚡',r:'epic',desc:l=>{const cd=[0,8,7.5,7,6.5,6][l],targets=[0,2,2,3,3,4][l],dmg=[0,2,3,4,5,6][l];return `Descarga a cada ${String(cd).replace('.',',')}s · até ${targets} alvos · ${dmg} de dano por alvo.`},apply:l=>{arcNextAt=Math.min(arcNextAt||Infinity,performance.now()+500)}},{id:'pierce',n:'Munição Perfurante',i:'🎯',r:'epic',desc:l=>{const every=[0,12,11,10,9,8][l],pass=[0,2,3,4,5,7][l];return `A cada ${every} tiros, 1 projétil atravessa até ${pass} inimigos.`},apply:l=>{}},{id:'phoenix',n:'Fênix',i:'🔥',r:'secret',desc:l=>'Skill única: revive 1x com 80% da vida máxima e 5s de proteção dourada.',apply:l=>{phoenixReady=true;phoenixConsumed=false}}];
const soldierSprite=new Image();let soldierReady=false;soldierSprite.onload=()=>soldierReady=true;soldierSprite.src='./assets/player/soldier-premium-01.png?v='+ASSET_TAG;
const ogreFrames={up:[],down:[],right:[],left:[]},eliteOgreFrames={up:[],down:[],right:[],left:[]},bossOgreFrames={up:[],down:[],right:[],left:[]},bossColossusFrames={up:[],down:[],right:[],left:[]},bossVoidFrames={up:[],down:[],right:[],left:[]};let ogreReady=false,eliteOgreReady=false,bossOgreReady=false,bossColossusReady=false,bossVoidReady=false;
async function cropAlphaFrame(img){const c=document.createElement('canvas'),x=c.getContext('2d',{willReadFrequently:true});c.width=img.naturalWidth||img.width;c.height=img.naturalHeight||img.height;x.drawImage(img,0,0);const d=x.getImageData(0,0,c.width,c.height).data;let minX=c.width,minY=c.height,maxX=-1,maxY=-1;for(let y=0;y<c.height;y++)for(let xx=0;xx<c.width;xx++)if(d[(y*c.width+xx)*4+3]>12){if(xx<minX)minX=xx;if(xx>maxX)maxX=xx;if(y<minY)minY=y;if(y>maxY)maxY=y}if(maxX<0)return img;const pad=2,x0=Math.max(0,minX-pad),y0=Math.max(0,minY-pad),w=Math.min(c.width-1,maxX+pad)-x0+1,h=Math.min(c.height-1,maxY+pad)-y0+1,o=document.createElement('canvas');o.width=w;o.height=h;o.getContext('2d').drawImage(c,x0,y0,w,h,0,0,w,h);return o}
async function loadDirectPngSequence(folder,count,cacheTag=ASSET_TAG){const jobs=[];for(let i=1;i<=count;i++){jobs.push((async()=>{const img=new Image(),name=`frame_${String(i).padStart(3,'0')}.png`;await new Promise((ok,fail)=>{img.onload=ok;img.onerror=()=>fail(Error(folder+'/'+name));img.src=`${folder}/${name}?v=${cacheTag}`});return(folder.includes('/mobs')||folder.includes('/weapons'))?await cropAlphaFrame(img):img})())}return Promise.all(jobs)}
async function loadNamedPngSequence(folder,prefix,count,cacheTag=ASSET_TAG){const jobs=[];for(let i=1;i<=count;i++){jobs.push((async()=>{const img=new Image(),name=`${prefix}${i}.png`;await new Promise((ok,fail)=>{img.onload=ok;img.onerror=()=>fail(Error(folder+'/'+name));img.src=`${folder}/${name}?v=${cacheTag}`});return img})())}return Promise.all(jobs)}
function mapDirect32(arr,target){if(arr.length<32)return false;target.down=[arr[0],arr[8],arr[16],arr[24]];target.dr=[arr[1],arr[9],arr[17],arr[25]];target.right=[arr[2],arr[10],arr[18],arr[26]];target.ur=[arr[3],arr[11],arr[19],arr[27]];target.up=[arr[4],arr[12],arr[20],arr[28]];target.ul=[arr[5],arr[13],arr[21],arr[29]];target.left=[arr[6],arr[14],arr[22],arr[30]];target.dl=[arr[7],arr[15],arr[23],arr[31]];return true}
async function loadOgrePack(path,target,bossMode=false){try{const r=await fetch(path+'?v='+ASSET_TAG,{cache:'no-store'});if(!r.ok)throw Error('HTTP '+r.status);const zip=await JSZip.loadAsync(await r.arrayBuffer());const names=Object.keys(zip.files).filter(n=>/\.png$/i.test(n)&&!zip.files[n].dir).sort((a,b)=>a.localeCompare(b,undefined,{numeric:true}));const urls={},ordered=[];for(const n of names){const blob=await zip.files[n].async('blob'),url=URL.createObjectURL(blob),img=new Image();await new Promise((ok,fail)=>{img.onload=ok;img.onerror=fail;img.src=url});urls[n]=img;ordered.push(img)}const by=(row,col)=>names.find(n=>n.includes(`recorte-${row}-${col}.png`));if(bossMode){const named=by(1,1)&&by(2,1)&&by(3,1)&&by(4,1);if(named){target.up=[1,2,3,4].map(c=>urls[by(1,c)]).filter(Boolean);target.down=[1,2,3,4].map(c=>urls[by(2,c)]).filter(Boolean);target.right=[1,2,3,4].map(c=>urls[by(3,c)]).filter(Boolean);target.left=[1,2,3,4].map(c=>urls[by(4,c)]).filter(Boolean)}else if(ordered.length>=16){if(path.includes('Ogroboss1.zip')){target.up=ordered.slice(0,4);target.down=ordered.slice(4,8);target.left=ordered.slice(8,12);target.right=ordered.slice(12,16)}else{target.up=ordered.slice(0,4);target.down=ordered.slice(4,8);target.right=ordered.slice(8,12);target.left=ordered.slice(12,16)}}else if(ordered.length>=4){target.up=[ordered[0]];target.down=[ordered[1]||ordered[0]];target.right=[ordered[2]||ordered[0]];target.left=[ordered[3]||ordered[0]]}}else{target.up=[1,2,3,4].map(row=>urls[by(row,1)]).filter(Boolean);target.down=[1,2,3,4].map(row=>urls[by(row,2)]).filter(Boolean);const side=[];for(let row=1;row<=4;row++)for(const col of[3,4]){const img=urls[by(row,col)];if(img)side.push(img)}target.right=side;target.left=side}return target.up.length>0&&target.down.length>0&&target.right.length>0&&target.left.length>0}catch(e){console.warn('Ogre pack indisponível',path,e);return false}}
(async()=>{const [colossus,voidlord]=await Promise.all([loadOgrePack('./assets/bosses/Ogroboss1.zip',bossColossusFrames,true),loadOgrePack('./assets/bosses/Ogro2.0Boss.zip',bossVoidFrames,true)]);bossColossusReady=colossus;bossVoidReady=voidlord;bossOgreReady=bossColossusReady||bossVoidReady;console.log('BOSS SKINS BACKGROUND READY',{colossus:bossColossusReady,voidlord:bossVoidReady})})();
const weaponSprite=new Image();let weaponReady=false;weaponSprite.onload=()=>weaponReady=true;weaponSprite.onerror=()=>weaponReady=false;weaponSprite.src='./assets/weapons/assault-rifle-01.png?v='+ASSET_TAG;
// PLAYER_V2_REAL_0138
const playerBaseFrames={up:[],ur:[],right:[],dr:[],down:[],dl:[],left:[],ul:[]},playerArmedFrames={up:[],ur:[],right:[],dr:[],down:[],dl:[],left:[],ul:[]},playerWeaponFrames={up:[],ur:[],right:[],dr:[],down:[],dl:[],left:[],ul:[]};
let playerV2Ready=false,weaponV2Ready=false;
let playerLoadFinished=false;function syncStartButton(){const b=$('startBtn');if(!b)return;if(playerV2Ready){b.disabled=false;b.textContent='ENTRAR NA ARENA';b.style.opacity='1'}else{b.disabled=true;b.textContent=playerLoadFinished?'SKIN DO PLAYER INDISPONÍVEL':'CARREGANDO PERSONAGEM...';b.style.opacity='.58'}}
async function prepZipImage(blob,removeWhite=false){const raw=URL.createObjectURL(blob),img=new Image();await new Promise((ok,fail)=>{img.onload=ok;img.onerror=fail;img.src=raw});const c=document.createElement('canvas'),cc=c.getContext('2d',{willReadFrequently:true});c.width=img.naturalWidth;c.height=img.naturalHeight;cc.drawImage(img,0,0);let id=cc.getImageData(0,0,c.width,c.height),d=id.data;if(removeWhite){for(let i=0;i<d.length;i+=4){const m=Math.min(d[i],d[i+1],d[i+2]);if(m>242)d[i+3]=0;else if(m>220)d[i+3]=Math.round(d[i+3]*(242-m)/22)}cc.putImageData(id,0,0);id=cc.getImageData(0,0,c.width,c.height);d=id.data}let minX=c.width,minY=c.height,maxX=-1,maxY=-1;for(let y=0;y<c.height;y++)for(let x=0;x<c.width;x++)if(d[(y*c.width+x)*4+3]>12){minX=Math.min(minX,x);maxX=Math.max(maxX,x);minY=Math.min(minY,y);maxY=Math.max(maxY,y)}if(maxX<0)return img;const pad=2,x0=Math.max(0,minX-pad),y0=Math.max(0,minY-pad),x1=Math.min(c.width-1,maxX+pad),y1=Math.min(c.height-1,maxY+pad),w=x1-x0+1,h=y1-y0+1,o=document.createElement('canvas');o.width=w;o.height=h;o.getContext('2d').drawImage(c,x0,y0,w,h,0,0,w,h);const out=new Image();await new Promise(ok=>{out.onload=ok;out.src=o.toDataURL('image/png')});return out}
async function zipImgs(path,removeWhite=false){const r=await fetch(path+'?v='+ASSET_TAG,{cache:'no-store'});if(!r.ok)throw Error(path+' HTTP '+r.status);const z=await JSZip.loadAsync(await r.arrayBuffer()),names=Object.keys(z.files).filter(n=>/\.png$/i.test(n)&&!z.files[n].dir).sort((a,b)=>a.localeCompare(b,undefined,{numeric:true,sensitivity:'base'})),arr=[];for(const n of names)arr.push(await prepZipImage(await z.files[n].async('blob'),removeWhite));return arr}
function mapPlayer32(arr,target){if(arr.length<32)return false;target.down=arr.slice(0,4);target.dr=arr.slice(4,8);target.right=arr.slice(8,12);target.ur=arr.slice(12,16);target.up=arr.slice(16,20);target.ul=arr.slice(20,24);target.left=arr.slice(24,28);target.dl=arr.slice(28,32);return true}
function mapWeapon16(arr,target){if(arr.length>=16){target.up=arr.slice(0,4);target.down=arr.slice(4,8);target.right=arr.slice(8,12);target.left=arr.slice(12,16)}else if(arr.length>=4){target.up=[arr[0]];target.down=[arr[1]||arr[0]];target.right=[arr[2]||arr[0]];target.left=[arr[3]||arr[0]]}}
(async()=>{
try{
  const base=await loadDirectPngSequence('./assets/player',32,ASSET_TAG);
  mapPlayer32(base,playerBaseFrames);
  playerV2Ready=playerBaseFrames.down.length===4&&playerBaseFrames.up.length===4&&playerBaseFrames.left.length===4&&playerBaseFrames.right.length===4;
  playerLoadFinished=true;syncStartButton();
  console.log('PLAYER BASE READY FAST',{base:base.length,ready:playerV2Ready});
}catch(e){playerLoadFinished=true;syncStartButton();console.error('PLAYER BASE PNG ERROR',e);return}
Promise.allSettled([
  loadNamedPngSequence('./assets/player-armed','Posearma',32,ASSET_TAG).then(armed=>{mapPlayer32(armed,playerArmedFrames);console.log('PLAYER ARMED BACKGROUND READY',armed.length)}),
  loadDirectPngSequence('./assets/weapons',32,ASSET_TAG).then(weap=>{mapPlayer32(weap,playerWeaponFrames);weaponV2Ready=playerWeaponFrames.down.length===4&&playerWeaponFrames.up.length===4&&playerWeaponFrames.left.length===4&&playerWeaponFrames.right.length===4;console.log('WEAPON BACKGROUND READY',weap.length)}),
  loadDirectPngSequence('./assets/mobs/Ogro',32,ASSET_TAG).then(mobFrames=>{if(mapDirect32(mobFrames,ogreFrames)){ogreReady=true;console.log('MOBS BACKGROUND READY',mobFrames.length)}}),
  loadDirectPngSequence('./assets/mobs/Ogro Elite',32,ASSET_TAG).then(eliteFrames=>{if(mapDirect32(eliteFrames,eliteOgreFrames)){eliteOgreReady=true;console.log('ELITE MOBS BACKGROUND READY',eliteFrames.length)}})
]);
})();
function playerFacing(a){const oct=Math.round((((a%(Math.PI*2))+Math.PI*2)%(Math.PI*2))/(Math.PI/4))%8;return ['right','dr','down','dl','left','ul','up','ur'][oct]}
function stableEnemyFacing8(e,vx,vy){const mag=Math.hypot(vx,vy);if(mag<.001)return e.facing||'down';const a=Math.atan2(vy,vx),oct=Math.round(a/(Math.PI/4)),dirs=['right','dr','down','dl','left','ul','up','ur'];const candidate=dirs[(oct+8)%8],now=performance.now();if(!e.facing){e.facing=candidate;e.faceCandidate='';e.faceCandidateAt=0;return e.facing}if(candidate===e.facing){e.faceCandidate='';e.faceCandidateAt=0;return e.facing}if(e.faceCandidate!==candidate){e.faceCandidate=candidate;e.faceCandidateAt=now;return e.facing}if(now-(e.faceCandidateAt||0)<120)return e.facing;e.facing=candidate;e.faceCandidate='';e.faceCandidateAt=0;return e.facing}
function stableEnemyFacing(e,vx,vy){const ax=Math.abs(vx),ay=Math.abs(vy);if(ax<.001&&ay<.001)return e.facing||'down';const candidate=ax>ay?(vx>0?'right':'left'):(vy>0?'down':'up'),now=performance.now();if(!e.facing){e.facing=candidate;e.faceCandidate='';e.faceCandidateAt=0;return e.facing}if(candidate===e.facing){e.faceCandidate='';e.faceCandidateAt=0;return e.facing}const major=Math.max(ax,ay),minor=Math.min(ax,ay),dominance=major/Math.max(.001,minor);if(dominance<1.32)return e.facing;if(e.faceCandidate!==candidate){e.faceCandidate=candidate;e.faceCandidateAt=now;return e.facing}if(now-(e.faceCandidateAt||0)<170)return e.facing;e.facing=candidate;e.faceCandidate='';e.faceCandidateAt=0;return e.facing}
function muzzleLocal(dir){const m={right:{x:46,y:-5},dr:{x:38,y:23},down:{x:5,y:35},dl:{x:-38,y:23},left:{x:-46,y:-5},ul:{x:-36,y:-30},up:{x:0,y:-46},ur:{x:36,y:-30}};return m[dir]||m.down}

function resize(){const r=stage.getBoundingClientRect();W=Math.max(320,r.width|0);H=Math.max(320,r.height|0);const mobile=W<=700,base=Math.min(devicePixelRatio||1,mobile?1.35:1.75);dpr=Math.max(.62,base*renderScale);canvas.width=Math.max(1,Math.floor(W*dpr));canvas.height=Math.max(1,Math.floor(H*dpr));draw()}addEventListener('resize',resize,{passive:true});for(const e of['contextmenu','selectstart','dragstart'])document.addEventListener(e,x=>x.preventDefault(),{passive:false});addEventListener('keydown',e=>keys[e.key.toLowerCase()]=1);addEventListener('keyup',e=>keys[e.key.toLowerCase()]=0);function ppos(e){const r=canvas.getBoundingClientRect();return{x:e.clientX-r.left,y:e.clientY-r.top}}canvas.onpointerdown=e=>{if(!running||paused||choosing)return;pointer=ppos(e);e.preventDefault()};canvas.onpointermove=e=>{if(pointer){pointer=ppos(e);e.preventDefault()}};canvas.onpointerup=canvas.onpointercancel=()=>pointer=null;canvas.setAttribute('draggable','false');canvas.tabIndex=-1;for(const ev of['touchstart','touchmove','gesturestart','gesturechange','gestureend']){canvas.addEventListener(ev,e=>e.preventDefault(),{passive:false})}function hash(x,y,s=0){let n=Math.imul(x,374761393)+Math.imul(y,668265263)+Math.imul(s,1442695041);n^=n>>>13;n=Math.imul(n,1274126177);return((n^(n>>>16))>>>0)/4294967295}function toast(t){toastText=t;toastUntil=performance.now()+1800}function ui(){const pct=Math.max(0,Math.min(100,Math.floor(xp/Math.max(1,xpNeed)*100)));$('level').textContent=level;$('xp').textContent=Math.floor(xp)+'/'+xpNeed;$('life').textContent=(Math.round(player.life*10)/10)+'/'+player.maxLife;$('mobCount').textContent=enemies.length;$('xpFill').style.width=pct+'%';const lp=$('levelPct');if(lp){lp.textContent=pct>=85?'QUASE LÁ · '+pct+'%':pct+'%';lp.classList.toggle('near',pct>=85)}}
function reset(){if(!playerV2Ready){syncStartButton();return}cancelAnimationFrame(raf);Object.assign(player,{x:0,y:0,life:100,maxLife:100,speed:255,inv:0,moving:false,walk:0,aim:0,shotFlash:0,fireRate:.28,xpMult:1,regen:0,flashDamage:0,bloodChance:0,bloodHeal:0,damage:2,armorReduction:0});for(const k in skillLv)skillLv[k]=0;score=0;level=1;xp=0;xpNeed=60;enemies=[];bullets=[];damageFx=[];pierceShotCounter=0;nextWaveAt=performance.now()+900;enemySpeed=1;freezeUntil=invincibleUntil=0;spawnTimer=.1;shotTimer=.05;paused=choosing=false;autoMode=false;hordeEnabled=true;killCount=0;medDrop=null;nextMedDropAt=performance.now()+180000;arcNextAt=0;arcFx=[];phoenixReady=false;phoenixConsumed=false;phoenixShieldUntil=0;autoFire=true;runStartedAt=performance.now();waveCount=0;deathState=null;autoTarget=null;autoTargetUntil=0;$('deathCam').classList.remove('show');document.querySelectorAll('.overlay').forEach(x=>x.classList.remove('show'));running=true;last=performance.now();ui();raf=requestAnimationFrame(loop);broadcast()}
function enemyTier(){const r=Math.random(),corrupt=level>=30?.12:level>=20?.08:level>=10?.035:0,elite=level>=30?.24:level>=15?.18:level>=5?.10:0;if(r<corrupt)return 2;if(r<corrupt+elite)return 1;return 0}function makeEnemy(type,near=false,forcedTier=null){if(enemies.length>=MAX_ENEMIES)return;const c=types[type];if(!c)return;const a=Math.random()*Math.PI*2,dist=near?180+Math.random()*220:Math.max(W,H)*.7+Math.random()*260,tier=c.boss?3:(forcedTier===1||forcedTier===2?forcedTier:enemyTier()),hpMult=tier===2?5:tier===1?3:1,dmgMult=tier===2?2.2:tier===1?1.7:1,xpMult=tier===2?6:tier===1?3.5:1;enemies.push({x:player.x+Math.cos(a)*dist,y:player.y+Math.sin(a)*dist,type,tier,r:c.r,speed:(c.s+Math.random()*8)*(tier===2?1.10:tier===1?1.05:1),hp:Math.ceil(c.h*hpMult),max:Math.ceil(c.h*hpMult),damage:Math.ceil(c.d*dmgMult),xp:Math.ceil(c.x*xpMult),dead:false,t:Math.random()*8,seed:Math.random()*99,attackAt:0,attackFlash:0,facing:'down',skinVariant:Math.floor(Math.random()*3),aiPhase:Math.floor(Math.random()*4),mvx:0,mvy:0})}function spawn(type,forcedTier=null){if(type)return makeEnemy(type,true,forcedTier);const pool=['wraith','reaper','infected','crawler','eye','brute'];makeEnemy(pool[Math.floor(Math.random()*pool.length)],false,forcedTier)}function spawnWave(){waveCount++;const softCap=Math.min(210,90+level*4),room=Math.max(0,softCap-enemies.length);if(room<=0)return;const n=Math.min(room,Math.min(18,4+Math.floor(level*.65)));for(let i=0;i<n;i++)spawn()}function boss(type){makeEnemy(type||(((Math.floor(level/10))%2)?'colossus':'voidlord'),true)}
function gainXP(v){xp+=v*player.xpMult;while(xp>=xpNeed){xp-=xpNeed;level++;xpNeed=Math.floor(60*Math.pow(level,1.42));if(level%10===0)boss(((level/10)%2===0)?'voidlord':'colossus');chooseSkill();break}ui()}function weightedPick(pool){let total=pool.reduce((s,x)=>s+rarityWeight[x.r],0),r=Math.random()*total;for(const x of pool){r-=rarityWeight[x.r];if(r<=0)return x}return pool[0]}function chooseSkill(){const pool=skills.filter(s=>s.id==='phoenix'?(!phoenixConsumed&&skillLv[s.id]<1):s.id==='armor'?skillLv[s.id]<4:skillLv[s.id]<5);if(!pool.length){toast('TODAS AS SKILLS ESTÃO NO LV5');return}choosing=true;const available=[...pool],pick=[];while(pick.length<Math.min(3,pool.length)){const s=weightedPick(available);pick.push(s);available.splice(available.indexOf(s),1)}if(autoMode){const scoreAuto=x=>{const id=x.id,cur=skillLv[id]||0;if(id==='phoenix')return phoenixConsumed?-999:120;if(id==='medic')return player.life/player.maxLife<.55?110:72;if(id==='regen')return player.life/player.maxLife<.7?92:68;if(id==='arc')return 88-cur*2;if(id==='flash')return 84-cur*2;if(id==='rapid')return 80-cur*2;if(id==='speed')return 72-cur*2;if(id==='blood')return 65-cur;if(id==='xp')return level<12?78:52;return 50};pick.sort((a,b)=>scoreAuto(b)-scoreAuto(a));const s=pick[0],cur=skillLv[s.id],next=s.id==='phoenix'?1:Math.min(5,cur+1);skillLv[s.id]=next;s.apply(next);choosing=false;toast('🤖 AUTO · '+s.n.toUpperCase()+' · LV '+next);last=performance.now();ui();broadcast();return}const box=$('skillChoices');box.innerHTML='';pick.forEach(s=>{const cur=skillLv[s.id],next=cur+1,b=document.createElement('button');b.className='skill '+s.r;b.innerHTML=`<span class="ico">${s.i}</span><span class="lvl">LV ${cur} → ${next}</span><b>${s.n}</b><em>${rarityLabel[s.r]}</em><small>${s.desc(next)}</small>`;b.onclick=()=>{skillLv[s.id]=next;s.apply(next);choosing=false;$('skillPick').classList.remove('show');toast(`${s.n.toUpperCase()} · LV ${next}`);last=performance.now();ui()};box.appendChild(b)});$('skillPick').classList.add('show')}
function addDamageFx(e,amount,kind='normal'){if(!e||!Number.isFinite(amount)||amount<=0)return;const now=performance.now(),last=damageFx.length?damageFx[damageFx.length-1]:null;if(last&&last.e===e&&now-last.at<115&&last.kind===kind){last.amount+=amount;last.at=now;return}damageFx.push({e,amount,kind,at:now,x:e.x,y:e.y});if(damageFx.length>48)damageFx.splice(0,damageFx.length-48)}function drawDamageFx(){const now=performance.now();damageFx=damageFx.filter(f=>now-f.at<720);for(const f of damageFx){const age=(now-f.at)/720,p=world(f.e&&!f.e.dead?f.e.x:f.x,f.e&&!f.e.dead?f.e.y:f.y),rise=10+age*24,alpha=Math.max(0,1-age);ctx.save();ctx.globalAlpha=alpha;ctx.textAlign='center';ctx.textBaseline='middle';ctx.font=(f.kind==='flash'||f.kind==='arc'?'900 12px':'900 11px')+' -apple-system,BlinkMacSystemFont,Segoe UI,sans-serif';ctx.lineWidth=3;ctx.strokeStyle='rgba(3,7,18,.92)';ctx.fillStyle=f.kind==='flash'?'#fde047':f.kind==='arc'?'#67e8f9':f.kind==='pierce'?'#f0abfc':'#ffffff';const txt='-'+(Math.round(f.amount*10)/10);ctx.strokeText(txt,p.x,p.y-rise);ctx.fillText(txt,p.x,p.y-rise);ctx.restore()}}function onKill(e){killCount++;score+=e.max>=100?500:e.tier===2?35:e.tier===1?20:10;gainXP(e.xp);if(skillLv.blood&&killCount%10===0){if(Math.random()<player.bloodChance){player.maxLife+=1;player.life+=1;toast('🩸 SANGUINÁRIO: +1 HP MÁXIMO')}else{player.life=Math.min(player.maxLife,player.life+player.bloodHeal);toast('🩸 SANGUINÁRIO: +'+String(player.bloodHeal).replace('.',',')+' HP')}}}function spawnMedDrop(){if(medDrop)return;const a=Math.random()*Math.PI*2,d=180+Math.random()*180;medDrop={x:player.x+Math.cos(a)*d,y:player.y+Math.sin(a)*d};toast('🩹 DROP MÉDICO NO MAPA')}function nearest(){let t=null,b=1e18;for(const e of enemies){if(e.dead)continue;const dx=e.x-player.x,dy=e.y-player.y,q=dx*dx+dy*dy;if(q<b){b=q;t=e}}return t}function targetVisible(e){if(!e||e.dead)return false;const dx=e.x-player.x,dy=e.y-player.y;return Math.abs(dx)<=W*.48&&Math.abs(dy)<=H*.48}const FIRE_RANGE=460;function nearestVisible(){let t=null,b=FIRE_RANGE*FIRE_RANGE;for(const e of enemies){if(e.dead||!targetVisible(e))continue;const dx=e.x-player.x,dy=e.y-player.y,q=dx*dx+dy*dy;if(q<b){b=q;t=e}}return t}const SWEEP_HALF_ANGLE=Math.PI/3;function angleDelta(a,b){return((a-b+Math.PI*3)%(Math.PI*2))-Math.PI}function sweepTarget(){let target=null,best=Infinity;const facing=Number.isFinite(movementAimAngle)?movementAimAngle:(player.aim||0);for(const e of enemies){if(e.dead||!targetVisible(e))continue;const dx=e.x-player.x,dy=e.y-player.y,dist=Math.hypot(dx,dy);if(dist>FIRE_RANGE)continue;const a=Math.atan2(dy,dx),delta=Math.abs(angleDelta(a,facing));if(delta>SWEEP_HALF_ANGLE)continue;const score=delta*900+dist*.18;if(score<best){best=score;target=e}}return target}function focusedTarget(){const now=performance.now(),near=nearestVisible();if(!near){autoTarget=null;autoTargetUntil=0;return null}if(autoTarget&&(!autoTarget.dead)&&enemies.includes(autoTarget)&&targetVisible(autoTarget)){const ad=Math.hypot(autoTarget.x-player.x,autoTarget.y-player.y),nd=Math.hypot(near.x-player.x,near.y-player.y);if(ad<=FIRE_RANGE&&now<autoTargetUntil&&nd>ad*.72)return autoTarget}autoTarget=near;autoTargetUntil=now+550;return autoTarget}function clearAutoTarget(){autoTarget=null;autoTargetUntil=0}function flash(){const a=player.aim,ux=Math.cos(a),uy=Math.sin(a);for(const e of enemies){const dx=e.x-player.x,dy=e.y-player.y,along=dx*ux+dy*uy,side=Math.abs(dx*uy-dy*ux);if(along>0&&along<520&&side<26+e.r){e.hp-=player.flashDamage;addDamageFx(e,player.flashDamage,'flash')}}bullets.push({flash:1,t:.16,angle:a})}function castArc(){const lv=skillLv.arc||0;if(!lv)return;const cds=[0,8,7.5,7,6.5,6],maxTargets=[0,2,2,3,3,4],damage=[0,2,3,4,5,6],now=performance.now();if(now<arcNextAt)return;const first=nearest();if(!first)return;arcNextAt=now+cds[lv]*1000;const hit=[first];while(hit.length<maxTargets[lv]){const last=hit[hit.length-1];let next=null,bd=190;for(const e of enemies){if(e.dead||hit.includes(e))continue;const d=Math.hypot(e.x-last.x,e.y-last.y);if(d<bd){bd=d;next=e}}if(!next)break;hit.push(next)}const pts=[{x:player.x,y:player.y},...hit.map(e=>({x:e.x,y:e.y}))];arcFx.push({pts,until:now+280});for(const e of hit){e.hp-=damage[lv];addDamageFx(e,damage[lv],'arc');if(e.hp<=0&&!e.dead){e.dead=true;onKill(e)}}toast('⚡ ARCO VOLTAICO · '+hit.length+' ALVOS')}function fmtRunTime(ms){const total=Math.max(0,Math.floor(ms/1000)),m=Math.floor(total/60),ss=total%60;return String(m).padStart(2,'0')+':'+String(ss).padStart(2,'0')}function liveUserLabel(v){v=String(v||'').trim();return v?'@'+v.replace(/^@/,''):''}function killerLabel(e){if(!e)return 'CAOS';const live=e.username||e.viewer||e.owner||e.sender||e.user;if(live)return '@'+String(live).replace(/^@/,'');return (types[e.type]?.name||'Mob desconhecido')+(e.tier===2?' · CORROMPIDO':e.tier===1?' · ELITE':'')}function beginDeath(e){if(deathState)return;const now=performance.now();enemies=enemies.filter(m=>m===e&&!m.dead);bullets=[];arcFx=[];medDrop=null;deathState={killer:e,at:now};$('deathKiller').textContent=killerLabel(e);$('deathLevel').textContent=level;$('deathTime').textContent=fmtRunTime(now-runStartedAt);$('deathWave').textContent=waveCount;$('deathKills').textContent=killCount;$('deathCam').classList.add('show');pointer=null;broadcast()}function tryPhoenix(){if(!phoenixReady||phoenixConsumed)return false;phoenixReady=false;phoenixConsumed=true;skillLv.phoenix=0;player.life=Math.max(1,player.maxLife*.8);player.inv=2;phoenixShieldUntil=performance.now()+5000;invincibleUntil=phoenixShieldUntil;for(const e of enemies){if(e.dead)continue;const dx=e.x-player.x,dy=e.y-player.y,d=Math.hypot(dx,dy)||1;if(d<190){const push=(190-d)*.8;e.x+=dx/d*push;e.y+=dy/d*push}}toast('🔥 FÊNIX · 80% HP · ESCUDO 5s');return true}function drawArcFx(){const now=performance.now();arcFx=arcFx.filter(f=>f.until>now);for(const f of arcFx){const alpha=Math.max(0,(f.until-now)/280);ctx.save();ctx.globalAlpha=alpha;ctx.strokeStyle='#a5f3fc';ctx.shadowColor='#22d3ee';ctx.shadowBlur=16;ctx.lineWidth=3;ctx.beginPath();for(let i=0;i<f.pts.length;i++){const p=world(f.pts[i].x,f.pts[i].y);if(i===0)ctx.moveTo(p.x,p.y);else{const prev=world(f.pts[i-1].x,f.pts[i-1].y),mx=(prev.x+p.x)/2+(Math.random()-.5)*12,my=(prev.y+p.y)/2+(Math.random()-.5)*12;ctx.lineTo(mx,my);ctx.lineTo(p.x,p.y)}}ctx.stroke();ctx.restore()}}function shoot(){let target=null;if(autoMode){target=focusedTarget();if(!target)return;player.aim=Math.atan2(target.y-player.y,target.x-player.x)}else if(gameplayMode==='sweep'){target=sweepTarget();if(!target)return;player.aim=Math.atan2(target.y-player.y,target.x-player.x)}else if(gameplayMode==='hardcore'){player.aim=movementAimAngle}else{target=nearestVisible();if(!target)return;player.aim=Math.atan2(target.y-player.y,target.x-player.x)}player.shotFlash=.1;const dir=playerFacing(player.aim),m=muzzleLocal(dir),pl=skillLv.pierce||0;let pierceLeft=0;if(pl){pierceShotCounter++;const every=[0,12,11,10,9,8][pl];if(pierceShotCounter>=every){pierceShotCounter=0;pierceLeft=[0,2,3,4,5,7][pl]}}bullets.push({x:player.x+m.x,y:player.y+m.y,vx:Math.cos(player.aim)*610,vy:Math.sin(player.aim)*610,r:4,dead:false,ammo:1,born:performance.now(),pierceLeft,hits:[]});if(player.flashDamage&&++flashCounter%5===0)flash()}function setPaused(v){paused=v;$('pause').classList.toggle('show',v);$('pauseBtn').textContent=v?'▶':'Ⅱ';pointer=null;broadcast()}function adminSkillReset(){for(const k in skillLv)skillLv[k]=0;Object.assign(player,{speed:255,fireRate:.28,xpMult:1,regen:0,flashDamage:0,bloodChance:0,bloodHeal:0,maxLife:100,armorReduction:0});player.life=Math.min(player.life,player.maxLife);arcNextAt=0;arcFx=[];phoenixReady=false;phoenixConsumed=false}function adminSkillApply(id,lv){lv=id==='phoenix'?(+lv>0?1:0):id==='armor'?Math.max(0,Math.min(4,+lv||0)):Math.max(0,Math.min(5,+lv||0));const sk=skills.find(x=>x.id===id);if(!sk)return false;skillLv[id]=lv;if(id==='phoenix'){phoenixReady=lv>0;phoenixConsumed=false;return true}if(id==='medic'){player.maxLife=100+lv*10;player.life=Math.min(player.maxLife,player.life);return true}if(lv>0)sk.apply(lv);else{if(id==='speed')player.speed=255;if(id==='rapid')player.fireRate=.28;if(id==='regen')player.regen=0;if(id==='armor')player.armorReduction=0;if(id==='xp')player.xpMult=1;if(id==='blood'){player.bloodChance=0;player.bloodHeal=0}if(id==='flash')player.flashDamage=0;if(id==='arc'){arcNextAt=0;arcFx=[]}}return true}function command(d){try{window.dispatchEvent(new CustomEvent('caos:admin-command',{detail:d}))}catch{}const c=d?.command;if(d?.user&&c){const lu=liveUserLabel(d.user),ln={spawn:'INVOCou INIMIGO',boss:'INVOCou BOSS',heal:'ATIVOU CURA',damage:'CAUSOU DANO',freeze:'CONGELOU A ARENA',invincible:'ATIVOU INVENCIBILIDADE',clear:'LIMPOU A ARENA',xp:'DEU XP',level:'DEU LEVEL'}[c];if(lu&&ln)toast('📡 '+lu+' · '+ln)}if(c==='pause')setPaused(true);if(c==='resume')setPaused(false);if(c==='heal')player.life=Math.min(player.maxLife,player.life+(+d.amount||1));if(c==='damage'){player.life=Math.max(0,player.life-(+d.amount||2));if(running&&player.life<=0&&!deathState){if(!tryPhoenix()){const k={username:d.user?String(d.user).replace(/^@/,''):'ADMIN',type:null,tier:0,x:player.x,y:player.y,r:20,dead:false};beginDeath(k)}}}if(c==='spawn')for(let i=0;i<Math.min(100,+d.amount||25);i++){const before=enemies.length,tier=d.tier===2||d.tier==='2'||d.tier==='corrupted'?2:d.tier===1||d.tier==='1'||d.tier==='elite'?1:null;spawn(d.mob||null,tier);if(enemies.length>before&&d.user)enemies[enemies.length-1].username=String(d.user).replace(/^@/,'')}if(c==='boss'){const qty=Math.max(1,Math.min(20,+d.amount||1));for(let i=0;i<qty;i++){const before=enemies.length;boss(d.mob||null);if(enemies.length>before&&d.user)enemies[enemies.length-1].username=String(d.user).replace(/^@/,'')}}if(c==='clear'){enemies=[];bullets=[]}if(c==='freeze')freezeUntil=performance.now()+(+d.seconds||8)*1000;if(c==='invincible')invincibleUntil=performance.now()+(+d.seconds||10)*1000;if(c==='speed')enemySpeed=Math.max(.2,Math.min(3,+d.value||1));if(c==='xp')gainXP(+d.amount||50);if(c==='level'){for(let i=0;i<(+d.amount||1);i++){xp=xpNeed;gainXP(0);if(choosing)break}}if(c==='auto'){autoMode=!!d.value;pointer=null;clearAutoTarget()}if(c==='autofire'){autoFire=!!d.value;player.shotFlash=0;shotTimer=.05;toast('🔫 TIRO AUTOMÁTICO '+(autoFire?'ON':'OFF'))}if(c==='gameplaymode'){const mode=String(d.value||'classic').toLowerCase();gameplayMode=['classic','sweep','hardcore'].includes(mode)?mode:'classic';movementAimAngle=player.aim||0;clearAutoTarget();toast(gameplayMode==='classic'?'🎮 JOGABILIDADE · CLÁSSICO':gameplayMode==='sweep'?'🎯 JOGABILIDADE · VARREDURA':'💀 JOGABILIDADE · HARDCORE')}if(c==='manualaim'){gameplayMode=d.value?'hardcore':'classic';movementAimAngle=player.aim||0;clearAutoTarget()}if(c==='horde'){hordeEnabled=!!d.value;spawnTimer=.1;toast('🌊 HORDAS '+(hordeEnabled?'ON':'OFF'))}if(c==='skilltest'){const id=String(d.skill||''),lv=id==='phoenix'?1:id==='armor'?Math.max(1,Math.min(4,+d.level||1)):Math.max(0,Math.min(5,+d.level||1));if(adminSkillApply(id,lv))toast('🧪 '+id.toUpperCase()+' · LV '+lv)}if(c==='skilltestall'){const lv=Math.max(1,Math.min(5,+d.level||5));for(const sk of skills)adminSkillApply(sk.id,sk.id==='armor'?Math.min(4,lv):lv);toast('🧪 TODAS · LV '+lv)}if(c==='skillmax'){for(const sk of skills)adminSkillApply(sk.id,sk.id==='armor'?4:5);toast('🧪 TODAS NO MÁXIMO')}if(c==='skillreset'){adminSkillReset();toast('🧪 HABILIDADES RESETADAS')}if(c==='ping')broadcast();if(c==='restart')reset();ui();broadcast()}
const SPATIAL=96;function buildEnemyGrid(){const g=new Map();for(const e of enemies){if(e.dead)continue;const cx=Math.floor(e.x/SPATIAL),cy=Math.floor(e.y/SPATIAL),k=cx+','+cy;let a=g.get(k);if(!a)g.set(k,a=[]);a.push(e)}return g}function separateEnemies(){const g=buildEnemyGrid();for(const e of enemies){if(e.dead)continue;const cx=Math.floor(e.x/SPATIAL),cy=Math.floor(e.y/SPATIAL);for(let oy=-1;oy<=1;oy++)for(let ox=-1;ox<=1;ox++){const a=g.get((cx+ox)+','+(cy+oy));if(!a)continue;for(const o of a){if(o===e||o.dead)continue;const dx=e.x-o.x,dy=e.y-o.y,d=Math.hypot(dx,dy)||.001,min=(types[e.type]?.boss||types[o.type]?.boss)?Math.max(34,(e.r+o.r)*.72):Math.max(22,(e.r+o.r)*.62);if(d<min){const push=Math.min(2.8,(min-d)*.14),ux=dx/d,uy=dy/d;e.x+=ux*push;e.y+=uy*push}}}}}function resolvePlayerMobCollisions(){let close=0;for(const e of enemies){if(e.dead)continue;const dx=e.x-player.x,dy=e.y-player.y,d=Math.hypot(dx,dy)||.001,min=player.r+e.r+3;if(d<min+13)close++}const crowded=close>=6,escapeBoost=close>=9?1.45:crowded?1.22:1;for(const e of enemies){if(e.dead)continue;let dx=e.x-player.x,dy=e.y-player.y,d=Math.hypot(dx,dy)||.001;const isBoss=!!types[e.type]?.boss,min=player.r+e.r+(isBoss?8:4);if(d>=min)continue;const ux=dx/d,uy=dy/d,overlap=Math.min(16,min-d);let mobShare=isBoss?.38:.82,playerShare=1-mobShare;if(crowded&&!isBoss){mobShare=.94;playerShare=.06}if(crowded&&isBoss){mobShare=.48;playerShare=.52}let ex=ux*overlap*mobShare*escapeBoost,ey=uy*overlap*mobShare*escapeBoost;if(player.moving&&!isBoss){const mx=player.moveX||0,my=player.moveY||0;const side=Math.sign(mx*uy-my*ux)||((e.seed||0)%2>.5?1:-1),tx=-my*side,ty=mx*side,lateral=overlap*(crowded?.52:.30);ex+=tx*lateral;ey+=ty*lateral}e.x+=ex;e.y+=ey;player.x-=ux*overlap*playerShare;player.y-=uy*overlap*playerShare}}function bulletHitsFromGrid(b,g){const cx=Math.floor(b.x/SPATIAL),cy=Math.floor(b.y/SPATIAL);for(let oy=-1;oy<=1;oy++)for(let ox=-1;ox<=1;ox++){const a=g.get((cx+ox)+','+(cy+oy));if(!a)continue;for(const e of a){if(e.dead||(b.hits&&b.hits.includes(e)))continue;const dx=b.x-e.x,dy=b.y-e.y,rr=b.r+e.r;if(dx*dx+dy*dy<rr*rr)return e}}return null}function autoVector(dt){
  let nearestMob=null,nearestDist=Infinity,vx=0,vy=0,threatCount=0,panic=false;
  for(const e of enemies){
    if(e.dead)continue;
    const dx=player.x-e.x,dy=player.y-e.y,d=Math.hypot(dx,dy)||1;
    if(d<nearestDist){nearestDist=d;nearestMob=e}
    const flee=(e.max>=100?360:245)+e.r;
    const critical=(e.max>=100?220:135)+e.r;
    if(d<flee){threatCount++;const w=Math.pow(1-d/flee,1.35)*(e.max>=100?1.8:1);vx+=dx/d*w;vy+=dy/d*w}
    if(d<critical)panic=true;
  }
  if(!nearestMob||!threatCount){autoMoveX*=.72;autoMoveY*=.72;if(Math.hypot(autoMoveX,autoMoveY)<.06){autoMoveX=0;autoMoveY=0}return{x:0,y:0}}
  if(Math.hypot(vx,vy)<.04){const dx=player.x-nearestMob.x,dy=player.y-nearestMob.y,d=nearestDist||1;vx=dx/d;vy=dy/d}
  const n=Math.hypot(vx,vy)||1,desiredX=vx/n,desiredY=vy/n,blend=panic?.42:.16;
  autoMoveX=autoMoveX*(1-blend)+desiredX*blend;autoMoveY=autoMoveY*(1-blend)+desiredY*blend;
  const sn=Math.hypot(autoMoveX,autoMoveY)||1;autoMoveX/=sn;autoMoveY/=sn;
  return{x:autoMoveX,y:autoMoveY}
}function update(dt){if(paused||choosing)return;if(deathState)return;let dx=(keys.d||keys.arrowright?1:0)-(keys.a||keys.arrowleft?1:0),dy=(keys.s||keys.arrowdown?1:0)-(keys.w||keys.arrowup?1:0);if(pointer){dx=pointer.x-W/2;dy=pointer.y-H/2;if(Math.hypot(dx,dy)<16)dx=dy=0}else if(autoMode&&!dx&&!dy){const v=autoVector(dt);dx=v.x;dy=v.y}const l=Math.hypot(dx,dy)||1;player.moving=!!(dx||dy);player.moveX=player.moving?dx/l:0;player.moveY=player.moving?dy/l:0;if(player.moving){player.x+=player.moveX*player.speed*dt;player.y+=player.moveY*player.speed*dt;player.walk+=dt*9;if(!autoMode&&(gameplayMode==='sweep'||gameplayMode==='hardcore'))movementAimAngle=Math.atan2(player.moveY,player.moveX)}if(!autoMode&&gameplayMode==='hardcore'){player.aim=movementAimAngle}else if(!autoMode&&gameplayMode==='sweep'){const target=autoFire?sweepTarget():null,wanted=target?Math.atan2(target.y-player.y,target.x-player.x):movementAimAngle;let da=angleDelta(wanted,player.aim);player.aim+=da*Math.min(1,dt*(target?8:11))}else if(autoFire){const target=autoMode?focusedTarget():nearestVisible();if(target){const wanted=Math.atan2(target.y-player.y,target.x-player.x);let da=angleDelta(wanted,player.aim);player.aim+=da*Math.min(1,dt*7)}}else if(player.moving){const wanted=Math.atan2(dy,dx);let da=angleDelta(wanted,player.aim);player.aim+=da*Math.min(1,dt*5)}player.inv=Math.max(0,player.inv-dt);player.shotFlash=Math.max(0,player.shotFlash-dt);if(player.regen>0&&player.life<player.maxLife){const safe=(performance.now()-lastDamageAt)/1000;if(safe>=1){const lv=skillLv.regen;let rate=player.regen;if(safe>=10)rate=[0,1,1.5,2,2.5,3.5][lv];else if(safe>=6)rate=[0,.8,1.2,1.6,2,2.5][lv];else if(safe>=3)rate=[0,.6,.9,1.2,1.5,1.8][lv];regenStage=safe>=10?3:safe>=6?2:safe>=3?1:0;player.life=Math.min(player.maxLife,player.life+rate*dt)}else regenStage=0;}$('coords').textContent='SETOR '+Math.floor(player.x/CHUNK)+', '+Math.floor(player.y/CHUNK);if(!medDrop&&performance.now()>=nextMedDropAt){spawnMedDrop();nextMedDropAt=Infinity}if(medDrop&&Math.hypot(medDrop.x-player.x,medDrop.y-player.y)<36){const heal=player.maxLife*.10;player.life=Math.min(player.maxLife,player.life+heal);toast('🩹 KIT MÉDICO: +'+(Math.round(heal*10)/10)+' HP');medDrop=null;nextMedDropAt=performance.now()+180000}if(hordeEnabled&&ogreReady){const now=performance.now(),softCap=Math.min(210,90+level*4);if(now>=nextWaveAt&&enemies.length<softCap){spawnWave();const pressure=enemies.length/Math.max(1,softCap),base=Math.max(1500,3600-level*55);nextWaveAt=now+base*(.9+pressure*.65)}}else{nextWaveAt=performance.now()+900}castArc();shotTimer-=dt;if(autoFire&&shotTimer<=0){shoot();shotTimer=player.fireRate}else if(!autoFire){shotTimer=Math.min(shotTimer,.05)}for(const b of bullets){if(b.flash){b.t-=dt;continue}b.x+=b.vx*dt;b.y+=b.vy*dt;if(Math.abs(b.x-player.x)>W||Math.abs(b.y-player.y)>H)b.dead=true}const frozen=performance.now()<freezeUntil;for(const e of enemies){e.t+=dt;if(!frozen){const dxp=player.x-e.x,dyp=player.y-e.y,dist2=dxp*dxp+dyp*dyp,near2=420*420,stride=dist2<near2?1:(perfMode>=2?4:perfMode===1?3:2);if(!e.mvx||((frameSeq+(e.aiPhase||0))%stride===0)){const distp=Math.sqrt(dist2)||1,baseA=Math.atan2(dyp,dxp);let steer=baseA;if(e.type==='crawler')steer+=Math.sin(e.t*7+e.seed)*.6;const stopRadius=player.r+e.r+(e.max>=100?58:38);if(distp<=stopRadius){e.mvx=0;e.mvy=0;e.speedMul=0;const fx=dxp,fy=dyp;(types[e.type]?.boss?stableEnemyFacing:stableEnemyFacing8)(e,fx,fy)}else{e.mvx=Math.cos(steer);e.mvy=Math.sin(steer);e.speedMul=distp<stopRadius+42?.68:1;(types[e.type]?.boss?stableEnemyFacing:stableEnemyFacing8)(e,e.mvx,e.mvy)}}e.x+=e.mvx*e.speed*enemySpeed*(1+level*.015)*dt*(e.speedMul||1);e.y+=e.mvy*e.speed*enemySpeed*(1+level*.015)*dt*(e.speedMul||1);} {const hitDist=Math.hypot(e.x-player.x,e.y-player.y),attackRange=player.r+e.r+(e.max>=100?58:38);if(hitDist<attackRange){const now=performance.now();if(now>=(e.attackAt||0)&&player.inv<=0&&now>invincibleUntil){e.attackAt=now+(e.max>=100?1100:900);e.attackFlash=.18;lastDamageAt=performance.now();player.life=Math.max(0,player.life-Math.max(1,e.damage*(1-player.armorReduction)));player.inv=.28;const kx=e.x-player.x,ky=e.y-player.y,kd=Math.hypot(kx,ky)||1;e.x+=kx/kd*8;e.y+=ky/kd*8;if(player.life<=0){if(!tryPhoenix())beginDeath(e)}}}}}separateEnemies();resolvePlayerMobCollisions();const enemyGrid=buildEnemyGrid();for(const b of bullets){if(b.flash||b.dead)continue;const e=bulletHitsFromGrid(b,enemyGrid);if(e){if(b.pierceLeft>0){b.hits.push(e);b.pierceLeft--;if(b.pierceLeft<=0)b.dead=true}else b.dead=true;e.hp-=player.damage;addDamageFx(e,player.damage,b.hits&&b.hits.length?'pierce':'normal');if(e.hp<=0){e.dead=true;onKill(e)}}}for(const e of enemies)if(e.hp<=0&&!e.dead){e.dead=true;onKill(e)}bullets=bullets.filter(b=>b.flash?b.t>0:!b.dead);enemies=enemies.filter(e=>!e.dead&&Math.abs(e.x-player.x)<W*2&&Math.abs(e.y-player.y)<H*2);ui()}
function world(x,y){let cx=player.x,cy=player.y;if(deathState?.killer){const k=deathState.killer,t=Math.min(1,(performance.now()-deathState.at)/900);cx=player.x+(k.x-player.x)*t*.62;cy=player.y+(k.y-player.y)*t*.62}return{x:x-cx+W/2,y:y-cy+H/2}}function terrain(){ctx.fillStyle='#08111e';ctx.fillRect(0,0,W,H);const ox=(((-player.x+W/2)%GRID)+GRID)%GRID,oy=(((-player.y+H/2)%GRID)+GRID)%GRID;ctx.lineWidth=1;ctx.strokeStyle='rgba(30,48,78,.34)';for(let x=ox;x<W;x+=GRID){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,H);ctx.stroke()}for(let y=oy;y<H;y+=GRID){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}const now=performance.now()/1000;ctx.save();ctx.globalCompositeOperation='screen';for(let i=0;i<7;i++){const px=hash(i,31)*W,py=hash(i,47)*H,r=80+hash(i,59)*150,drift=Math.sin(now*.08+i)*8;const g=ctx.createRadialGradient(px+drift,py,0,px+drift,py,r);g.addColorStop(0,'rgba(50,70,105,.035)');g.addColorStop(1,'rgba(7,13,24,0)');ctx.fillStyle=g;ctx.beginPath();ctx.arc(px+drift,py,r,0,Math.PI*2);ctx.fill()}for(let i=0;i<18;i++){const px=(hash(i,71)*W+now*(2+hash(i,73)*3))%W,py=(hash(i,79)*H+Math.sin(now*.12+i)*3+H)%H;ctx.fillStyle='rgba(249,115,22,'+(0.08+hash(i,83)*0.12)+')';ctx.beginPath();ctx.arc(px,py,.45+hash(i,89)*.8,0,Math.PI*2);ctx.fill()}ctx.restore()}
function tierAura(e,img,w,h,isBoss){if(!img||isBoss||e.tier<1)return;const pulse=.82+Math.sin(e.t*3.2)*.18,sets=e.tier===1?[['#050816',18],['#3b82f6',12],['#a855f7',7]]:[['#000000',22],['#7f1d1d',14],['#ef4444',8]];ctx.save();ctx.globalCompositeOperation='source-over';for(const [color,blur] of sets){ctx.save();ctx.globalAlpha=.40*pulse;ctx.shadowColor=color;ctx.shadowBlur=perfMode>=2?Math.max(4,blur*.45):perfMode===1?blur*.7:blur;ctx.drawImage(img,-w/2,-h*.72,w,h);ctx.restore()}ctx.restore()}
function mobSkinFrame(img,v){return img}function drawOgreSkin(e,isBoss){const pack=isBoss?(e.type==='colossus'?bossColossusFrames:e.type==='voidlord'?bossVoidFrames:bossOgreFrames):(e.tier===1&&eliteOgreReady?eliteOgreFrames:ogreFrames),ready=isBoss?(e.type==='colossus'?bossColossusReady:e.type==='voidlord'?bossVoidReady:bossOgreReady):(e.tier===1&&eliteOgreReady?eliteOgreReady:ogreReady);let dir=e.facing||'down';const arr=pack[dir]||pack.down||[];if(!ready||!arr.length)return false;let img=arr[(e.speedMul===0?0:Math.floor(e.t/(isBoss?.15:.135)))%arr.length]||pack.down[0];if(!img)return false;if(!isBoss)img=mobSkinFrame(img,e.skinVariant||0);const h=isBoss?e.r*3.55:(e.tier===1?67:62),ratio=(img.naturalWidth&&img.naturalHeight)?img.naturalWidth/img.naturalHeight:1,sideScale=!isBoss?(dir==='left'||dir==='right'?.82:(dir==='ul'||dir==='ur'||dir==='dl'||dir==='dr'?.90:1)):1,w=h*ratio*sideScale;if(!isBoss)tierAura(e,img,w,h,false);ctx.save();ctx.imageSmoothingEnabled=true;ctx.drawImage(img,-w/2,-h*.72,w,h);ctx.restore();return true}
function drawEnemy(e,p){const c=types[e.type],isBoss=!!c.boss;ctx.save();ctx.translate(p.x,p.y);ctx.fillStyle='#0008';ctx.beginPath();ctx.ellipse(0,e.r*.9,e.r*(isBoss?1.15:.95),isBoss?8:6,0,0,7);ctx.fill();ctx.shadowColor=isBoss?'#ef4444':c.c;ctx.shadowBlur=isBoss?(perfMode>=2?8:perfMode===1?14:24):0;drawOgreSkin(e,isBoss);ctx.shadowBlur=0;const barY=isBoss?-e.r*2.05:-e.r*1.72,barW=e.r*(isBoss?2.55:2.1);if(e.hp<e.max||isBoss||e.tier>0){ctx.fillStyle='#17070d';ctx.fillRect(-barW/2,barY,barW,6);ctx.fillStyle=isBoss?'#f59e0b':e.tier===2?'#ef4444':e.tier===1?'#a855f7':'#fb7185';ctx.fillRect(-barW/2,barY,barW*Math.max(0,e.hp/e.max),6)}if(isBoss){ctx.fillStyle='#fde68a';ctx.font='bold 8px sans-serif';ctx.textAlign='center';ctx.fillText(c.name,0,barY-7)}if(performance.now()<freezeUntil){const sec=Math.ceil((freezeUntil-performance.now())/1000);ctx.fillStyle='#dff6ff';ctx.font='bold 9px sans-serif';ctx.textAlign='center';ctx.fillText('❄ '+sec+'s',0,barY-17)}ctx.restore()}
function drawPlayer(){
  ctx.save();ctx.translate(W/2,H/2);
  ctx.fillStyle='#0009';ctx.beginPath();ctx.ellipse(0,25,23,7,0,0,Math.PI*2);ctx.fill();
  const a=player.aim||0,dir=playerFacing(a),frame=player.moving?Math.floor(player.walk)%4:0,bob=player.moving?Math.sin(player.walk*Math.PI*.5)*.8:0;
  if(playerV2Ready){
    const armedReady=playerArmedFrames.down.length===4,pack=(autoFire&&armedReady)?playerArmedFrames:playerBaseFrames,arr=pack[dir]?.length?pack[dir]:pack.down,img=arr[frame%arr.length]||arr[0];
    const h=80,bodyRatio=(img.naturalWidth&&img.naturalHeight)?img.naturalWidth/img.naturalHeight:.8,w=Math.min(78,h*bodyRatio),bottom=36+bob;
    const weaponLayout={
      down:{x:2,y:-1,maxW:30,maxH:50,flip:false},dr:{x:14,y:-6,maxW:52,maxH:31,flip:false},right:{x:17,y:-5,maxW:56,maxH:28,flip:true},ur:{x:13,y:-12,maxW:50,maxH:31,flip:false},
      up:{x:0,y:-18,maxW:30,maxH:50,flip:false},ul:{x:-13,y:-12,maxW:50,maxH:31,flip:false},left:{x:-17,y:-5,maxW:56,maxH:28,flip:false},dl:{x:-14,y:-6,maxW:52,maxH:31,flip:false}
    };
    const drawWeapon=()=>{if(!autoFire||!weaponV2Ready)return;const wa=playerWeaponFrames[dir]?.length?playerWeaponFrames[dir]:playerWeaponFrames.down,wi=wa[frame%wa.length]||wa[0];if(!wi)return;const q=weaponLayout[dir]||weaponLayout.down,iw=wi.naturalWidth||wi.width||1,ih=wi.naturalHeight||wi.height||1,ratio=iw/Math.max(1,ih);let ww=q.maxW,wh=ww/Math.max(.05,ratio);if(wh>q.maxH){wh=q.maxH;ww=wh*ratio}ctx.save();ctx.imageSmoothingEnabled=true;ctx.translate(q.x,q.y+bob);if(q.flip)ctx.scale(-1,1);ctx.drawImage(wi,-ww/2,-wh/2,ww,wh);ctx.restore()};
    const weaponBehind=autoFire&&['up','ur','ul'].includes(dir);
    if(weaponBehind)drawWeapon();
    ctx.save();ctx.imageSmoothingEnabled=true;ctx.drawImage(img,-w/2,bottom-h,w,h);ctx.restore();
    if(!weaponBehind)drawWeapon();
    if(autoFire&&player.shotFlash>0){const m=muzzleLocal(dir);ctx.save();ctx.globalCompositeOperation='screen';ctx.fillStyle='#fff7b2';ctx.shadowColor='#f59e0b';ctx.shadowBlur=14;ctx.beginPath();ctx.arc(m.x,m.y+bob,4.2,0,Math.PI*2);ctx.fill();ctx.restore()}
    ctx.restore();return;
  }
  ctx.restore();return;
}function drawPhoenixShield(){const now=performance.now();if(now>=phoenixShieldUntil)return;const left=(phoenixShieldUntil-now)/2000,pulse=.5+.5*Math.sin(now/90),fade=Math.min(1,left*3);ctx.save();ctx.translate(W/2,H/2);ctx.globalAlpha=fade;ctx.globalCompositeOperation='screen';const glow=ctx.createRadialGradient(0,0,20,0,0,54);glow.addColorStop(0,'rgba(255,220,90,.12)');glow.addColorStop(.55,'rgba(255,153,0,.08)');glow.addColorStop(1,'rgba(255,90,0,0)');ctx.fillStyle=glow;ctx.beginPath();ctx.arc(0,0,54,0,Math.PI*2);ctx.fill();ctx.shadowColor='#f59e0b';ctx.shadowBlur=20+8*pulse;ctx.lineWidth=3;ctx.strokeStyle='#fde68a';ctx.setLineDash([13,7]);ctx.lineDashOffset=-now/28;ctx.beginPath();ctx.arc(0,0,42+pulse*2,0,Math.PI*2);ctx.stroke();ctx.shadowColor='#fb923c';ctx.shadowBlur=16;ctx.lineWidth=2;ctx.strokeStyle='#fb923c';ctx.setLineDash([5,10]);ctx.lineDashOffset=now/22;ctx.beginPath();ctx.arc(0,0,34-pulse,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);for(let i=0;i<7;i++){const a=now/360+i*Math.PI*2/7,r=48+(i%2)*4;const x=Math.cos(a)*r,y=Math.sin(a)*r;ctx.shadowColor='#fbbf24';ctx.shadowBlur=12;ctx.fillStyle=i%2?'#fb923c':'#fde047';ctx.beginPath();ctx.arc(x,y,1.8+(i%3)*.45,0,Math.PI*2);ctx.fill()}ctx.restore()}function drawShield(){if(performance.now()>=invincibleUntil)return;const left=(invincibleUntil-performance.now())/1000,p=.5+.5*Math.sin(performance.now()/120),blink=left<3?(Math.floor(performance.now()/160)%2?.25:1):1;ctx.save();ctx.globalAlpha=blink;ctx.translate(W/2,H/2);ctx.strokeStyle='#7dd3fc';ctx.shadowColor='#38bdf8';ctx.shadowBlur=18;ctx.lineWidth=2.5;ctx.setLineDash([10,6]);ctx.lineDashOffset=-performance.now()/35;ctx.beginPath();ctx.arc(0,0,39+p*2,0,7);ctx.stroke();ctx.restore()}function drawMed(){if(!medDrop)return;const p=world(medDrop.x,medDrop.y),pulse=1+.08*Math.sin(performance.now()/180);ctx.save();ctx.translate(p.x,p.y);ctx.scale(pulse,pulse);ctx.shadowColor='#22c55e';ctx.shadowBlur=18;ctx.fillStyle='#f8fafc';ctx.fillRect(-14,-11,28,22);ctx.fillStyle='#ef4444';ctx.fillRect(-4,-8,8,16);ctx.fillRect(-9,-3,18,6);ctx.shadowBlur=0;ctx.fillStyle='#86efac';ctx.font='bold 9px sans-serif';ctx.textAlign='center';ctx.fillText('KIT +10%',0,-18);ctx.restore()}function drawFreeze(){if(performance.now()>=freezeUntil)return;const now=performance.now(),g=ctx.createRadialGradient(W/2,H/2,20,W/2,H/2,Math.max(W,H)*.7);g.addColorStop(0,'rgba(56,189,248,.02)');g.addColorStop(1,'rgba(186,230,253,.18)');ctx.fillStyle=g;ctx.fillRect(0,0,W,H);ctx.strokeStyle='rgba(186,230,253,.85)';ctx.lineWidth=3;ctx.strokeRect(5,5,W-10,H-10);for(let i=0;i<18;i++){const x=(hash(i,7)*W+now*(5+hash(i,9)*8)/1000)%W,y=(hash(i,11)*H+now*(9+hash(i,13)*12)/1000)%H;ctx.fillStyle='rgba(224,242,254,.75)';ctx.beginPath();ctx.arc(x,y,1.5,0,7);ctx.fill()}}
function draw(){if(!W||!H)return;ctx.setTransform(dpr,0,0,dpr,0,0);terrain();for(const b of bullets){if(b.flash){const a=b.angle,fade=b.t/.16,x1=W/2+Math.cos(a)*28,y1=H/2+Math.sin(a)*28,x2=W/2+Math.cos(a)*520,y2=H/2+Math.sin(a)*520;ctx.save();ctx.globalAlpha=fade;ctx.strokeStyle='#fffde7';ctx.shadowColor='#fef08a';ctx.shadowBlur=24;ctx.lineWidth=16;ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke();ctx.restore()}else{const p=world(b.x,b.y),a=Math.atan2(b.vy,b.vx);ctx.save();ctx.translate(p.x,p.y);ctx.rotate(a);const trail=ctx.createLinearGradient(-22,0,5,0);trail.addColorStop(0,'rgba(34,211,238,0)');trail.addColorStop(.55,'rgba(34,211,238,.22)');trail.addColorStop(1,'rgba(125,211,252,.8)');ctx.fillStyle=trail;ctx.beginPath();ctx.moveTo(-22,-2.2);ctx.lineTo(2,-3.5);ctx.lineTo(5,0);ctx.lineTo(2,3.5);ctx.lineTo(-22,2.2);ctx.closePath();ctx.fill();ctx.shadowColor='#38bdf8';ctx.shadowBlur=10;ctx.fillStyle='#e0f2fe';ctx.beginPath();ctx.roundRect(-2,-3.2,15,6.4,3);ctx.fill();ctx.fillStyle='#fff';ctx.beginPath();ctx.moveTo(13,-3.2);ctx.lineTo(20,0);ctx.lineTo(13,3.2);ctx.closePath();ctx.fill();ctx.shadowBlur=0;ctx.restore()}}for(const e of enemies){const p=world(e.x,e.y),m=e.r+46;if(p.x<-m||p.x>W+m||p.y<-m||p.y>H+m)continue;drawEnemy(e,p)}drawMed();drawArcFx();for(const le of enemies){if(le.dead||!le.username)continue;const lp=world(le.x,le.y),lu='@'+String(le.username).replace(/^@/,'');ctx.save();ctx.font='800 9px -apple-system,BlinkMacSystemFont,Segoe UI,sans-serif';ctx.textAlign='center';ctx.textBaseline='middle';const lw=ctx.measureText(lu).width+12,lh=17,lx=lp.x-lw/2,ly=lp.y-le.r-31;ctx.fillStyle='rgba(5,8,18,.86)';ctx.strokeStyle='rgba(139,92,246,.72)';ctx.lineWidth=1;ctx.beginPath();ctx.roundRect(lx,ly,lw,lh,8);ctx.fill();ctx.stroke();ctx.fillStyle='#ddd6fe';ctx.fillText(lu,lp.x,ly+lh/2+.5);ctx.restore()}drawDamageFx();drawPlayer();drawPhoenixShield();if(performance.now()>=phoenixShieldUntil)drawShield();drawFreeze();if(performance.now()<toastUntil){ctx.save();ctx.fillStyle='rgba(3,7,18,.82)';ctx.beginPath();ctx.roundRect(W/2-125,H-75,250,34,14);ctx.fill();ctx.fillStyle='#fff';ctx.font='bold 11px sans-serif';ctx.textAlign='center';ctx.fillText(toastText,W/2,H-53);ctx.restore()}}
function tunePerformance(t){perfFrames++;if(t-perfWindowStart<800)return;perfLastFps=Math.max(1,Math.round(perfFrames*1000/(t-perfWindowStart)));window.__caosFps=perfLastFps;perfFrames=0;perfWindowStart=t;const mobs=enemies.length;let next=perfMode;if(perfLastFps<40||mobs>=165)next=2;else if(perfLastFps<53||mobs>=90)next=Math.max(1,perfMode);else if(perfLastFps>58&&mobs<70)next=0;else if(perfLastFps>55&&mobs<110&&perfMode===2)next=1;if(next!==perfMode){perfMode=next;const target=perfMode===2?.56:perfMode===1?.76:1;if(Math.abs(renderScale-target)>.01){renderScale=target;resize()}}}function loop(t){if(!running)return;frameSeq++;tunePerformance(t);const dt=Math.min(.033,(t-last)/1000);last=t;update(dt);draw();raf=requestAnimationFrame(loop)}const deathClose=$('deathClose');if(deathClose)deathClose.onclick=()=>{if(!deathState)return;const now=performance.now();$('deathCam').classList.remove('show');$('finalText').textContent='Level '+level+' · '+score+' pontos · '+killCount+' abates · '+fmtRunTime(now-runStartedAt);$('over').classList.add('show');running=false;broadcast()};const startButton=$('startBtn');if(startButton){syncStartButton();startButton.onclick=()=>reset()}$('restartBtn').onclick=()=>reset();$('pauseBtn').onclick=()=>setPaused(!paused);$('resumeBtn').onclick=()=>setPaused(false);
const alphabet='ABCDEFGHJKLMNPQRSTUVWXYZ23456789',makeCode=()=>Array.from({length:8},()=>alphabet[Math.floor(Math.random()*alphabet.length)]).join(''),params=new URLSearchParams(location.search),room=(params.get('sala')||sessionStorage.getItem('chaos-room')||makeCode()).toUpperCase();sessionStorage.setItem('chaos-room',room);$('roomCode').textContent=room;let peer,retryTimer=null,peerGeneration=0,connections=new Set();function status(t,on=false){$('remoteStatus').textContent=t;$('connectionMini').textContent='PAINEL: '+t;$('connectionMini').style.color=on?'#22c55e':'#7681aa'}function state(){const now=performance.now(),alive=enemies.filter(e=>!e.dead),elite=alive.filter(e=>e.tier===1).length,corrupted=alive.filter(e=>e.tier===2).length,bosses=alive.filter(e=>types[e.type]?.boss).length;return{type:'state',version:VERSION,running,paused,health:Math.round(player.life*10)/10,maxHealth:player.maxLife,score,mobs:alive.length,kills:killCount,elapsedMs:runStartedAt?Math.max(0,now-runStartedAt):0,elite,corrupted,bosses,level,xp:Math.floor(xp),xpNeed,autoMode,hordeEnabled,autofire:autoFire,gameplayMode,manualAim:gameplayMode==='hardcore',wave:waveCount,fps:Math.max(0,Math.round(window.caosCurrentFps||0)),perfMode,skillLv:{...skillLv},room,ts:Date.now()}}function broadcast(){const s=state();for(const c of [...connections])if(c.open)try{c.send(s)}catch{}}function scheduleGamePeer(gen){if(gen!==peerGeneration||retryTimer)return;retryTimer=setTimeout(()=>{retryTimer=null;openPeer()},2500)}function openPeer(){clearTimeout(retryTimer);retryTimer=null;const gen=++peerGeneration;try{peer?.destroy()}catch{}connections.clear();if(typeof Peer==='undefined')return status('PEERJS INDISPONÍVEL');peer=new Peer('chaos-live-'+room.toLowerCase(),{debug:0});peer.on('open',()=>{if(gen===peerGeneration)status('AGUARDANDO PAINEL')});peer.on('connection',c=>{if(gen!==peerGeneration){c.close();return}connections.add(c);c.on('open',()=>{if(gen===peerGeneration){status('CONECTADO',true);c.send(state())}});c.on('data',d=>{if(gen===peerGeneration&&d?.type==='command')command(d)});c.on('close',()=>{connections.delete(c);if(gen===peerGeneration&&connections.size===0)status('AGUARDANDO PAINEL')});c.on('error',()=>connections.delete(c))});peer.on('error',e=>{if(gen!==peerGeneration||e?.type==='unavailable-id')return;status('RECONECTANDO...');scheduleGamePeer(gen)});peer.on('disconnected',()=>{if(gen!==peerGeneration)return;try{peer.reconnect()}catch{scheduleGamePeer(gen)}})}setInterval(broadcast,500);openPeer();resize();draw()})();

(()=>{'use strict';let visible=true,frames=0,last=performance.now(),fps=0;try{visible=localStorage.getItem('caos-show-fps')!=='0'}catch{}let el=document.getElementById('fpsMini');if(!el){const host=document.getElementById('connectionMini');if(host){el=document.createElement('div');el.id='fpsMini';el.style.marginTop='3px';host.parentNode.insertBefore(el,host.nextSibling)}}function paint(){if(el){el.style.display=visible?'block':'none';el.textContent='FPS: '+(fps||'--')}}function tick(t){frames++;if(t-last>=500){fps=Math.max(0,Math.round(frames*1000/(t-last)));frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){visible=!!d.value;try{localStorage.setItem('caos-show-fps',visible?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();

(()=>{'use strict';let on=true,frames=0,last=performance.now(),fps=0;try{on=localStorage.getItem('caos-show-fps')!=='0'}catch{}const el=document.getElementById('fpsHud');function paint(){if(!el)return;el.style.display=on?'inline-block':'none';el.textContent='FPS '+(fps||'--')}function tick(t){frames++;if(t-last>=500){fps=Math.round(frames*1000/(t-last));window.caosCurrentFps=fps;frames=0;last=t;paint()}requestAnimationFrame(tick)}window.addEventListener('caos:admin-command',e=>{const d=e.detail||{};if(d.command==='fps'){on=!!d.value;try{localStorage.setItem('caos-show-fps',on?'1':'0')}catch{}paint()}});paint();requestAnimationFrame(tick)})();
